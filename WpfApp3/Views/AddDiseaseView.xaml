<Window x:Class="WpfApp3.Views.AddDiseaseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp3.Views" xmlns:viewmodels="clr-namespace:WpfApp3.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:AddDiseaseViewModel}"
        mc:Ignorable="d"
        Title="Добавить болезнь" Height="400" Width="454" Icon="/Views/Ico.png">

    <Grid>

        <StackPanel Grid.Column="0" Margin="10">
            <TextBlock Text="Номер болезни:"  Margin="10"/>
            <TextBox x:Name="DiseaseIDTextBox" Text="{Binding DiseaseID}" />
            <TextBlock Text="Наименование болезни:" Margin="10"/>
            <TextBox x:Name="DiseaseNameTextBox" Text="{Binding DiseaseName}" />
            <TextBlock Text="Дата постановки диагноза:"  Margin="10"/>
            <TextBox x:Name="DiagnosisDateTextBox" Text="{Binding DDate, StringFormat=dd-MM-yyyy}" Width="434"/>
            <Button Content="Добавить болезнь" Command="{Binding AddDiseaseCommand}" Margin="0,20,0,0" />
            <TextBlock Text="{Binding ResultTextBlock}" Foreground="Red" Margin="0,30,0,0" Height="69"/>
        </StackPanel>

    </Grid>
</Window>
